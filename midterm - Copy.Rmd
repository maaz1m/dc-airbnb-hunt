---
title: "Untitled"
output: html_document
---

```{r ,results="T"}
knitr::opts_chunk$set(echo = TRUE)
library(ezids)

setwd("C:/Users/Atharva/Documents/GitHub/airbnb-analysis")
getwd()
midterm <- data.frame(read.csv("listings_detailed.csv"))
```

```{r , results = 'T' }
library(ggplot2)
#histogram for room type
ggplot(data = midterm, aes(room_type))+ stat_count(width = 0.5,col="red",fill="#03FDFC",alpha = 1) + labs(title = "Room type vs Frequency", x= "Room Type", y= "Frequency")
#Histogram and QQ plot for Review scores
review_score_noout = outlierKD2(midterm, review_scores_rating, rm=TRUE, boxplt=FALSE, histogram=TRUE, qqplt=TRUE) 

#HIstogram and QQ plot for Reviews per month
reviews_pm_noout = outlierKD2(midterm, reviews_per_month, rm=TRUE, boxplt=FALSE, histogram=TRUE, qqplt=TRUE)

```
.
